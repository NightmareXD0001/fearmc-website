
/**
 * This file is empty as we're not using the custom cursor effect anymore
 * Keeping the file to avoid import errors but not implementing any functionality
 */

export const initCustomCursor = () => {
  // Empty function that returns a cleanup function
  return () => {};
};
